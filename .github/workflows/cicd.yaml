name: CICD pipeline
on: workflow_dispatch

jobs:
  lint-and-format:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          sparse-checkout: |
            webserver
      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: 'webserver/go.mod'
          cache-dependency-path: 'webserver/go.sum'
          cache: true
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.9.0
          working-directory: webserver

  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          sparse-checkout: |
            webserver
      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: 'webserver/go.mod'
          cache-dependency-path: 'webserver/go.sum'
          cache: true
      - name: Run Go test coverage
        working-directory: webserver
        run: go test -cover ./...

  scan-repo:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      security-events: write
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        env:
          TRIVY_DEBUG: true
        with:
          sparse-checkout: |
            webserver
            trivy.yaml
      - name: Scan repository for vulnerabilities (generate SBOM)
        uses: aquasecurity/trivy-action@0.34.0
        with:
          scan-type: 'fs'
          scan-ref: 'webserver'
          trivy-config: '${{ github.workspace }}/trivy.yaml'
          format: 'github'
          output: '${{ github.workspace }}/dependency-results.sbom.json'
          github-pat: ${{ secrets.GITHUB_TOKEN }}

  scan-infra:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        env:
          TRIVY_DEBUG: true
        with:
          sparse-checkout: |
            iac
            argocd-apps
            trivy.yaml
      - name: Scan IaC for vulnerabilities
        uses: aquasecurity/trivy-action@0.34.0
        with:
          scan-type: 'fs'
          scan-ref: '.'
          #skip-dirs: 'iac'  # Workaround as Trivy does panic here
          trivy-config: '${{ github.workspace }}/trivy.yaml'
          scanners: 'vuln,misconfig,secret'
          format: 'sarif'
          output: '${{ github.workspace }}/trivy-iac-results.sarif'
      - name: Upload SARIF scan results to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: '${{ github.workspace }}/trivy-iac-results.sarif'

  build-scan-push-image:
    needs: [lint-and-format, test, scan-repo, scan-infra]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      security-events: write
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          sparse-checkout: |
            webserver
            trivy.yaml
      - name: Login to registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3
      - name: Container image metadata
        uses: docker/metadata-action@v5
        id: meta
        with:
          images: ghcr.io/testdotcom/webserver
          tags: |
            type=ref,event=tag
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha
      - name: Build container image
        uses: docker/bake-action@v6
        id: build
        with:
          push: false
          load: true
          workdir: webserver
          source: "{{defaultContext}}:webserver"
          files: |
            ./docker-bake.hcl
            cwd://${{ steps.meta.outputs.bake-file-tags }}
          targets: build
          set: |
            *.output=type=docker
      - name: Scan container image for vulnerabilities
        uses: aquasecurity/trivy-action@0.34.0
        env:
          TRIVY_DEBUG: true
        with:
          image-ref: 'ghcr.io/testdotcom/webserver:latest'
          scan-type: 'image'
          trivy-config: '${{ github.workspace }}/trivy.yaml'
          format: 'sarif'
          output: '${{ github.workspace }}/trivy-img-results.sarif'
      - name: Upload SARIF scan results to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: '${{ github.workspace }}/trivy-img-results.sarif'
      - name: Push container image
        if: success()
        uses: docker/bake-action@v6
        with:
          push: true
          workdir: webserver
          source: "{{defaultContext}}:webserver"
          files: |
            ./docker-bake.hcl
            cwd://${{ steps.meta.outputs.bake-file-tags }}
          targets: build
